const getDynamicLinkUrl = () => {
  return "";
};

const createDynamicLinkUrl = (url: string) => {
  return "success";
};

const share = () => {
  return true;
};

export default { getDynamicLinkUrl, createDynamicLinkUrl, share };
